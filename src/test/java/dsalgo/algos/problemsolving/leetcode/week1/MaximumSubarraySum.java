package dsalgo.algos.problemsolving.leetcode.week1;

/**
 * Created by vedikagupta on 24/05/20
 * <p>
 * The naive method of running two for loops will have the complexity O(n^2)
 * Using the "Divide & Conquer" approach will have O(nLog(n)) time complexity
 * <p>
 * The naive method of running two for loops will have the complexity O(n^2)
 * Using the "Divide & Conquer" approach will have O(nLog(n)) time complexity
 */

/**
 * The naive method of running two for loops will have the complexity O(n^2)
 * Using the "Divide & Conquer" approach will have O(nLog(n)) time complexity
 */

/**
 * Divide the array in 2 halves
 * Return the max subarray sum in left half
 * Return the max subarray sum in right half
 * Max subarray sum such that the subarray crosses the midpoint
 */

public class MaximumSubarraySum {
    public static void main(String[] args) {
        int[] nums = {-2, 1, -3, 4, -1, 2, 1, -5, 4};

    }

    private int getSum(int[] arr, int l, int h) {
        return 0;
    }
}
